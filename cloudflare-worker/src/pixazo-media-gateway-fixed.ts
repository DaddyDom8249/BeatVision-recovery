import { resolveShotstackSource } from './shotstack-gateway';
const BASE='https://gateway.pixazo.ai';const STATUS=`${BASE}/v2/requests/status/`;const CONTRACT='1.1';const LTX_TIMEOUT_MS=300000;const LTX_POLL_INTERVAL_MS=7000;
const cors=(r:Request,e:any)=>{const origin=r.headers.get('Origin')||'';const allowed=String(e.ALLOWED_ORIGIN||'').split(',').map((x:string)=>x.trim()).filter(Boolean);return{'Access-Control-Allow-Origin':origin&&(!allowed.length||allowed.includes(origin))?origin:(allowed[0]||'*'),'Vary':'Origin','Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type,X-BeatVision-Contract,Authorization,X-BeatVision-Request'}};
const json=(r:Request,e:any,data:unknown,status=200)=>new Response(JSON.stringify(data,null,2),{status,headers:{'Content-Type':'application/json',...cors(r,e)}});const auth=(r:Request,e:any)=>!e.GATEWAY_TOKEN||r.headers.get('Authorization')===`Bearer ${e.GATEWAY_TOKEN}`;const media=(d:any)=>d?.output?.media_url?.[0]||d?.output?.media_url||d?.output||d?.imageUrl||d?.image_url||d?.url||null;const imageItems=(p:any)=>{const values=p?.images?.images||p?.images||[];return Array.isArray(values)?values.filter((item:any)=>item?.image_url||item?.url||item?.data_url):[]};const clip=(v:unknown,n:number)=>String(v??'').slice(0,n);
const worldPrompt=(p:any,scene?:any)=>{const w=p?.world||{};return clip(['BeatVision cinematic music-video artwork.','Dark industrial realism, coherent recurring character and environment, cinematic lighting.','No text, logos, watermarks, captions, UI or typography.',p?.style?`Style: ${clip(p.style,500)}`:'',`Character: ${clip(JSON.stringify(w.character_concept||{}),900)}`,`Locations: ${clip(JSON.stringify(w.locations||[]),700)}`,`Motifs: ${clip(JSON.stringify(w.visual_motifs||[]),700)}`,`Continuity: ${clip(JSON.stringify(w.continuity_rules||[]),500)}`,scene?`Scene: ${clip(JSON.stringify(scene),700)}`:'Create a strong world establishing image.'].filter(Boolean).join('\n'),2048)};
async function call(path:string,key:string,body:any,model:string){const response=await fetch(`${BASE}${path}`,{method:'POST',headers:{'Content-Type':'application/json','Cache-Control':'no-cache','Ocp-Apim-Subscription-Key':key},body:JSON.stringify(body)});const text=await response.text();let data:any;try{data=JSON.parse(text)}catch{data={raw:text}}if(!response.ok)throw new Error(`Pixazo ${model} ${response.status} at ${path}: ${String(data?.message||data?.error||text).slice(0,1800)}`);return data}
async function wait(key:string,requestId:string,model:string){const deadline=Date.now()+LTX_TIMEOUT_MS;while(Date.now()<deadline){const response=await fetch(`${STATUS}${encodeURIComponent(requestId)}`,{headers:{'Ocp-Apim-Subscription-Key':key,'Cache-Control':'no-cache'}});const text=await response.text();let data:any;try{data=JSON.parse(text)}catch{data={}}if(!response.ok)throw new Error(`Pixazo ${model} status ${response.status}: ${text.slice(0,1200)}`);const state=String(data?.status||'').toUpperCase();if(state==='COMPLETED'||state==='SUCCEEDED'){const url=media(data);if(!url)throw new Error(`Pixazo ${model} completed without media output.`);return url}if(['ERROR','FAILED','CANCELLED'].includes(state))throw new Error(`Pixazo ${model} job ${state}: ${String(data?.error||'unknown provider error').slice(0,1600)}`);await new Promise(resolve=>setTimeout(resolve,LTX_POLL_INTERVAL_MS))}throw new Error(`Pixazo ${model} job timed out after ${LTX_TIMEOUT_MS/1000} seconds.`)}
async function img(key:string,model:'flux-schnell'|'sdxl',promptText:string,aspect:'square'|'wide'='square'){const data=model==='flux-schnell'?await call('/flux-1-schnell/v1/getData',key,{prompt:clip(promptText,2048),num_steps:4,height:1024,width:1024},model):await call('/getImage/v1/getSDXLImage',key,{prompt:clip(promptText,12000),height:aspect==='wide'?576:1024,width:1024,num_steps:20,guidance:5},model);const url=media(data);if(!url)throw new Error(`Pixazo ${model} returned no image URL.`);return url}
async function labeled(label:string,work:()=>Promise<string>){try{return await work()}catch(error){throw new Error(`${label}: ${error instanceof Error?error.message:String(error)}`)}}
export default {async fetch(r:Request,e:any){if(r.method==='OPTIONS')return new Response(null,{status:204,headers:cors(r,e)});const id=r.headers.get('X-BeatVision-Request')||crypto.randomUUID();if(!auth(r,e))return json(r,e,{ok:false,error:'Unauthorized',request_id:id},401);if(r.method!=='POST')return json(r,e,{ok:false,error:'POST required',request_id:id},405);const key=e.PIXAZO_API_KEY;if(!key)return json(r,e,{ok:false,contract_version:CONTRACT,status:'provider_unavailable',provider:'pixazo',request_id:id,error:'PIXAZO_API_KEY is not configured.'},503);let body:any;try{body=await r.json()}catch{return json(r,e,{ok:false,error:'Invalid JSON body',request_id:id},400)}if(body?.contract_version!==CONTRACT)return json(r,e,{ok:false,error:'Expected BeatVision contract 1.1.',request_id:id},400);const payload=body.payload||{};try{if(body.operation==='worldAssets'){const character=await labeled('character / Flux Schnell',()=>img(key,'flux-schnell',worldPrompt(payload)+'\nFocus on the primary recurring character.'));const environment=await labeled('environment / Flux Schnell',()=>img(key,'flux-schnell',worldPrompt(payload)+'\nFocus on the defining environment.'));const hero=await labeled('hero / SDXL',()=>img(key,'sdxl',worldPrompt(payload)+'\nCreate a polished establishing keyframe.'));return json(r,e,{ok:true,contract_version:CONTRACT,capability:'image',provider:'pixazo',model:'flux-1-schnell+sdxl',request_id:id,result:{characters:[{name:'Primary BeatVision character',image_url:character}],environments:[{name:'Primary BeatVision environment',image_url:environment}],hero_image_url:hero,models_used:['flux-1-schnell','sdxl']}})}if(body.operation==='sceneImages'){const scenes=Array.isArray(payload?.storyboard?.scenes)?payload.storyboard.scenes.slice(0,24):[];if(!scenes.length)return json(r,e,{ok:false,status:'invalid_input',request_id:id,error:'Storyboard contains no scenes.'},400);const images=[];for(let i=0;i<scenes.length;i++){const scene=scenes[i],data=await call('/getImage/v1/getSDXLImage',key,{prompt:clip(worldPrompt(payload,scene),12000),height:576,width:1024,num_steps:20,guidance:5},'sdxl'),url=media(data);if(!url)throw new Error(`Pixazo SDXL scene ${i+1} returned no image URL.`);images.push({scene:Number(scene.scene||i+1),status:'generated',image_url:url,model:'sdxl'})}return json(r,e,{ok:true,contract_version:CONTRACT,capability:'image',provider:'pixazo',model:'sdxl',request_id:id,result:{images,models_used:['sdxl']}})}return json(r,e,{ok:false,error:`Pixazo route not implemented for operation: ${body.operation}`,request_id:id},404)}catch(error){return json(r,e,{ok:false,contract_version:CONTRACT,provider:'pixazo',status:'provider_error',request_id:id,error:String(error instanceof Error?error.message:error).slice(0,2000)},502)}}};